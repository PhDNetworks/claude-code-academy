# Custom Commands

This folder contains custom Claude Code slash commands.

**Module:** 6 - Custom Commands

**How it works:**
- Each `.md` file becomes a command (e.g., `audit.md` → `/audit`)
- Use `$ARGUMENTS` for runtime parameters

**Planned commands:**
- `audit.md` - Code analysis for security/performance
- `test-gen.md` - Generate unit tests
- `docs.md` - Generate JSDoc comments
