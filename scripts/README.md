# Scripts

This folder contains SDK experiments and automation scripts.

**Module:** 13 - The Claude Code SDK

**How it works:**
- Use `@anthropic-ai/claude-code` npm package
- Same tools as terminal, but programmatic
- Default read-only, allowTools for write access

**Planned scripts:**
- `claude-helper.js` - Basic SDK usage example
- `review-changes.js` - Automated code review
